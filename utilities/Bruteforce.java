package utilities;

import java.util.LinkedList;
import java.util.List;

public class Bruteforce {

	public List<String> stringsForSizeWith(int size, boolean digits, boolean alphabets, boolean specials) {
		if(size > 6){
			return null;
		}
		List<String> bruteList = new LinkedList<>();
		/*
		 * Generate Strings as per requirements
		 */
		return bruteList;
	}
	
	// A method that will call another method with strings for size range and datas required not provide a List of String
	// will stop when callable method returns true
}
